/* eslint-disable no-unused-vars */
/* global beforeAll beforeEach document expect test */

// This file will run tests that are Travis-friendly.
// In order to run Selenium tests, follow these steps:
//   1. Change the name of this file (e.g., to "index.test-travis.js")
//   2. Change the name of "index.test-selenium.js" to "index.text.js"
//   3. Run "yarn test"

// require('isomorphic-fetch');
// const index = require('./index');

// beforeAll(() => {
//   index.constructDocument();
// });

// test('Constructs outerContainer', () => {
//   expect(document.getElementById('outerContainer')).not.toEqual(null);
// });

// test('Constructs innerContainer', () => {
//   expect(document.getElementById('innerContainer')).not.toEqual(null);
// });
