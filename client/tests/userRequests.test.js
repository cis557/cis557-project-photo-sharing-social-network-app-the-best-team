/* global */

import {
  getUser,
  getOtherUser,
  getSuggestedUsers,
}
  from '../src/javascripts/userRequests';

describe('Baseline tests', () => {
  test('Runs the testing suite', () => {
    expect(1).toEqual(1);
  });
});
