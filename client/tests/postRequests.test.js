/* global */

import {
  addPost,
  editPost,
  getPost,
  getFeed,
  getLikes,
  deletePost,
}
  from '../src/javascripts/postRequests';

describe('Baseline tests', () => {
  test('Runs the testing suite', () => {
    expect(1).toEqual(1);
  });
});
