/* global */

import {
  follow,
  unfollow,
}
  from '../src/javascripts/followRequests';

describe('Baseline tests', () => {
  test('Runs the testing suite', () => {
    expect(1).toEqual(1);
  });
});
